name: M360

description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  image_picker: ^1.0.7
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^6.0.0


# ✅ FLUTTER LAUNCHER ICON CONFIG (ROOT LEVEL)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "images/icon.png"


  remove_alpha_ios: true

flutter:
  uses-material-design: true

  assets:
    - images/


